# Blockinfo Varlink service
interface com.redhat.blkinfo

# Returns a list of block devices in the system after applying filters
method GetDisksJson(filters_json: ?string) -> (disks_json: string)

# Stop serving
method StopServing() -> ()

# Something failed in TestMore
error TestMoreError (reason: string)
